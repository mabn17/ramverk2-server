build:
  nodes:
    environment:
        mongodb: true
    coverage:
      tests:
        override:
          - command: npm run test:ci
            coverage:
              file: .coverage/clover.xml
              format: clover